<html>

<head>
    <title>
        <%= title %>
    </title>
    <style>
        * {
            margin: 0;
            padding: 0;
            font-family: "Poppins", sans-serif;
        }

        body {
            width: 100%;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .wrapper {
            width: 380px;
            padding: 40px 30px 50px 30px;
            background: rgb(28, 149, 255);
            border-radius: 20px;
            text-align: center;
            box-shadow: 10px 10px 15px rgba(0, 0, 0, 0.1);
        }

        .wrapper header {
            color: white;
            font-size: 35px;
            font-weight: 600;
        }

        button {
            height: 50px;
            margin-top: 30px;
            color: #fff;
            padding: 0;
            border: none;
            background: #3d78cf;
            cursor: pointer;
            border-bottom: 2px solid rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            width: 100%;
            outline: none;
            font-size: 18px;
            border-radius: 5px;
        }

        button:hover {
            background: rgb(31, 78, 144);
        }
    </style>
</head>

<body>
    <div class="wrapper">
        <header>
            <%= title %>
        </header>
        <button onclick="location.href='/login'">進入系統</button>
    </div>
</body>

</html>